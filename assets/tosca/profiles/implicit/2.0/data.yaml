tosca_definitions_version: tosca_2_0

metadata:

  puccini.scriptlet.import:tosca.comparer.version: js/comparers/version.js
  puccini.scriptlet.import:tosca.validation._format: js/constraints/$format.js

data_types:

  #
  # Primitive
  #

  boolean:
    metadata:
      puccini.type: ard.boolean
      specification.citation: '[TOSCA-v2.0]'
      specification.location: '?'

  integer:
    metadata:
      puccini.type: ard.integer
      specification.citation: '[TOSCA-v2.0]'
      specification.location: '?'

  float:
    metadata:
      puccini.type: ard.float
      specification.citation: '[TOSCA-v2.0]'
      specification.location: '?'

  string:
    metadata:
      puccini.type: ard.string
      specification.citation: '[TOSCA-v2.0]'
      specification.location: '?'

  bytes:
    metadata:
      puccini.type: bytes
      specification.citation: '[TOSCA-v2.0]'
      specification.location: '?'

  nil:
    metadata:
      puccini.type: ard.null
      specification.citation: '[TOSCA-v2.0]'
      specification.location: '?'

  #
  # Special
  #

  version:
    metadata:
      puccini.type: version
      puccini.comparer: tosca.comparer.version
      specification.citation: '[TOSCA-v2.0]'
      specification.location: '?'

  timestamp:
    metadata:
      puccini.type: timestamp
      specification.citation: '[TOSCA-v2.0]'
      specification.location: '?'

  range:
    metadata:
      puccini.type: range
      specification.citation: '[TOSCA-v2.0]'
      specification.location: '?'

  tosca.datatypes.json?:
    metadata:
      tosca.normative: 'true'
      specification.citation: '[TOSCA-v2.0]'
      specification.location: '?'
    # ERRATUM: typo
    description: >-
      The json type is a TOSCA data Type used to define a string that contains data in the
      JavaScript Object Notation (JSON) format.
    derived_from: string
    validation: { _format: json }

  tosca.datatypes.xml?:
    metadata:
      tosca.normative: 'true'
      specification.citation: '[TOSCA-v2.0]'
      specification.location: '?'
    # ERRATUM: typo
    description: >-
      The xml type is a TOSCA data Type used to define a string that contains data in the
      Extensible Markup Language (XML) format.
    derived_from: string
    validation: { _format: xml }

  #
  # Scalar
  #
  
  scalar:
    metadata:
      puccini.type: scalar
      specification.citation: '[TOSCA-v2.0]'
      specification.location: '9.1.2.2'
    description: >-
      The scalar type is an abstract TOSCA data type used to define scalar values 
      along with an associated unit. It cannot be used directly but must be refined 
      into a concrete scalar type.

  #
  # Collections
  #

  list:
    metadata:
      puccini.type: ard.list
      specification.citation: '[TOSCA-v2.0]'
      specification.location: '?'

  map:
    metadata:
      puccini.type: ard.map
      specification.citation: '[TOSCA-v2.0]'
      specification.location: '?'
